/**
 * ExplorerPage — 라우트 진입점 (드릴다운 탐색기)
 *
 * Feature slice의 ExplorerPage를 re-export한다.
 * routeConfig.tsx에서 lazy import로 사용.
 */

export { ExplorerPage } from '@/features/explorer/components/ExplorerPage';
