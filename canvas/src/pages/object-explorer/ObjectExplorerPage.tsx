/**
 * ObjectExplorerPage — 라우트 진입점
 *
 * Feature slice의 ObjectExplorerPage를 re-export한다.
 * routeConfig.tsx에서 lazy import로 사용.
 */

export { ObjectExplorerPage } from '@/features/object-explorer/components/ObjectExplorerPage';
