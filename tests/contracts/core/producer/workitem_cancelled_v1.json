{
  "event_name": "WORKITEM_CANCELLED",
  "version": "1.0.0",
  "producer": "core",
  "schema": {
    "type": "object",
    "required": ["workitem_id", "proc_inst_id", "tenant_id", "idempotency_key"],
    "properties": {
      "workitem_id": {"type": "string"},
      "proc_inst_id": {"type": "string"},
      "tenant_id": {"type": "string"},
      "cancellation_reason": {"type": "string"},
      "idempotency_key": {"type": "string"},
      "event_contract": {"type": "object"}
    }
  }
}
